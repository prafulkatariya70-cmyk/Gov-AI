[pytest]
markers =
    integration: marks tests that require external resources or services

# Keep the repository's deterministic xdist configuration.
required_plugins = pytest-xdist
addopts = -n 2 --dist loadscope
